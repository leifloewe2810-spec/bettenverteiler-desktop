{
  "name": "bettenverteiler-desktop",
  "version": "0.1.3",
  "private": true,
  "description": "IPAS Bettenverteiler - Desktop-App (Tauri) fuer Mac + Windows",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:mac": "tauri build --bundles dmg",
    "build:win": "tauri build --bundles nsis"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.2"
  }
}
